{
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "https://RAILWAY_URL/api/*"
    },
    {
      "route": "/*",
      "allowedRoles": ["anonymous", "authenticated"]
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*", "*.css", "*.js", "*.json"]
  }
}
